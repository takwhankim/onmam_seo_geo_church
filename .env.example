# Public values are safe to expose in the browser.
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
NEXT_PUBLIC_APP_NAME="Vercel Auto Deploy Starter"

# Server-only secrets must be filled in locally and in Vercel Project Settings.
# Do not commit real secret values.
DATABASE_URL=""
API_SECRET_KEY=""
